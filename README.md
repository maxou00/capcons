# CapCons


check `src/styles/globals.scss` for css variables. Many of them sit there.
theming is in `src/theme/index.ts`.
Could not find `Visby CF` font. So I used `Visby` family (Regular, Bold, ....). Check it in `assets/fonts/visby`

